import styled from 'styled-components';

export const AnalyzeEntryStyles = styled.div`
  cursor: pointer;

  .iconfont.icon-pinpu-show-lyrics {
    color: ${(props) => props.theme.textColor.gray};
  }
`;
